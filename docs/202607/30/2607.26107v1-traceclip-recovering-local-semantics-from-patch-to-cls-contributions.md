---
title: "TraceCLIP: Recovering Local Semantics from Patch-to-CLS Contributions"
title_zh: TraceCLIP：从补丁到CLS贡献中恢复局部语义
authors: "Xinran Liu, Shouqian Shi, Yutong Chen, Ge Wang, Xin-Wei Yao, Sheng Zhong"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.26107v1"
tags: ["query:open-vocab-d"]
score: 8.0
evidence: 通过CLIP进行开放词汇语义分割
tldr: CLIP的全局对比学习目标导致局部语义缺失，现有方法依赖额外监督或训练。TraceCLIP通过从CLS注意力输出中追踪patch贡献，无训练地恢复局部语义，并利用语义测地线门校准最终层patch亲和度。在8个零样本语义分割基准上，平均mIoU比最强无训练方法提升1.3-4.5点。研究表明全局对齐的内部结构中仍可提取空间定位语义。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26107v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 871, \"height\": 710, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26107v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 816, \"height\": 546, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26107v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1837, \"height\": 815, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26107v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 787, \"height\": 731, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26107v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 864, \"height\": 791, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26107v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1282, \"height\": 662, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26107v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 645, \"height\": 570, \"label\": \"Table\"}]"
motivation: CLIP的全局表示缺乏显式局部语义关联，现有方法需额外监督、外部模型或任务适配。
method: 从CLS注意力输出隔离patch特定贡献，再通过语义测地线门校准patch特征进行密集重建。
result: 在8个零样本语义分割数据集上，平均mIoU提升1.3-4.5点，超越所有无训练方法。
conclusion: 证明无需额外训练即可从CLIP全局对齐中提取局部语义，为密集理解提供新路径。
---

## 摘要
密集视觉-语言理解，包括目标定位、区域识别和开放词汇语义分割，需要将语言概念与空间定位的视觉区域关联起来。CLIP通过大规模对比预训练学习共享的图像-文本嵌入空间，为这些任务提供了坚实基础。然而，其图像级目标将文本与CLS衍生的全局表示对齐，导致局部视觉-语言对应关系仅受到间接约束。现有方法或引入额外监督、外部模型、任务特定适配，而无训练方法主要从现有补丁特征恢复密集响应，而未检查局部语义在CLIP中最易获取的位置。我们提出TraceCLIP，一种无训练框架，通过隔离写入CLS注意力输出中的补丁特定项来恢复潜在补丁级语义证据。TraceCLIP进一步将贡献衍生的语义响应转换为语义测地拓扑门，该门校准最终层补丁亲和性以进行密集特征重建。诊断实验表明，这些贡献特征展现出强局部语义判别性和文本条件空间对齐。在八个零样本语义分割基准上，TraceCLIP在不同主干和背景设置下，相较于先前最强的无训练方法，平均mIoU提升1.3至4.5个百分点，且无需额外训练、外部视觉基础模型或区域级监督。更广泛地，这些发现表明空间定位语义可能在全局对齐表示的内部构造中仍然可访问。

## Abstract
Dense vision-language understanding, including object localization, region recognition, and open-vocabulary semantic segmentation, requires associating language concepts with spatially grounded visual regions. CLIP provides a strong foundation for these tasks by learning a shared image-text embedding space from large-scale contrastive pre-training. However, its image-level objective aligns text with a CLS-derived global representation, leaving local vision-language correspondence only indirectly constrained. Existing methods either introduce additional supervision, external models, or task-specific adaptation, while training-free approaches mainly recover dense responses from existing patch features without examining where local semantics become most accessible within CLIP. We introduce TraceCLIP, a training-free framework that recovers latent patch-level semantic evidence by isolating the patch-specific terms written into the CLS attention output. TraceCLIP further converts contribution-derived semantic responses into a semantic-geodesic topology gate that calibrates final-layer patch affinity for dense feature reconstruction. Diagnostic experiments show that these contribution features exhibit strong local semantic discrimination and text-conditioned spatial alignment. On eight zero-shot semantic segmentation benchmarks, TraceCLIP achieves gains of 1.3 to 4.5 points in average mIoU over the strongest prior training-free methods across both backbones and background settings, without additional training, external vision foundation models, or region-level supervision. More broadly, these findings suggest that spatially localized semantics may remain accessible within the internal construction of globally aligned representations.

---

## 论文详细总结（自动生成）

# 论文总结：TraceCLIP: Recovering Local Semantics from Patch-to-CLS Contributions

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在ViT-based CLIP模型中，图像级对比学习目标将文本与CLS全局表示对齐，导致补丁级别的局部语义与语言之间的对应关系仅受间接约束。现有的密集视觉-语言理解方法（目标定位、区域识别、语义分割）大多需要引入额外监督、外部模型或任务特定适配，而训练自由的方法主要聚焦于如何从现有补丁特征中变换出更好的密集响应，并未探究CLIP内部计算中局部语义证据最直接可访问的位置。
- **整体含义**：论文旨在回答一个更基础的问题——在CLIP的全局对齐表示内部构造中，局部语义是否仍然可访问？若可以，如何恢复？作者通过诊断实验发现，patch-to-CLS贡献路径中包含了清晰且空间定位的语义证据，且这些证据在残差混合后变得不易直接访问。由此提出TraceCLIP，无训练地利用该路径恢复局部语义，从而改善密集预测。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：利用CLIP自注意力中CLS对每个补丁的注意力权重及相应的值变换，从CLS注意力输出中隔离出每个补丁的贡献项，并将其读入CLIP共享嵌入空间，得到可直接与文本嵌入比较的局部特征。进一步将这些局部语义响应转化为语义-测地线拓扑门，用于校准最终层的补丁亲和度，从而重建空间一致的密集特征。
- **关键技术细节**：
  1. **Patch-to-CLS贡献分解**：在第l层第h头，定义贡献特征 \(C^{l,h}_i = W_{proj} \left[ A^{l,h}_{0i} W^{l,h}_{OV} \text{LN}(x^l_i) \right]\)，其中 \(A^{l,h}_{0i}\) 是CLS到补丁i的注意力权重，\(W^{l,h}_{OV}\) 是值到输出的投影，\(W_{proj}\) 是CLIP视觉投影。跨头聚合得 \(C^l_i = \sum_{h=1}^{H} C^{l,h}_i\)。
  2. **语义先验构建**：对每个补丁i，用贡献特征与类别文本嵌入计算相似度 \(S_{con} = C^T T\)，取最大响应类别作为初步语义标签 \(L_i\)，softmax概率作为置信度 \(P_i\)。
  3. **语义-测地线拓扑门**：在4邻域网格图上基于标签和置信度定义边代价 \(w_{ij} = 1 + \beta m_{ij} c_{ij}\)（\(m_{ij}=1\)若标签不同，\(c_{ij}=\min(P_i,P_j)\)）。计算全对最短路径距离 \(D_{ij}\)，再转化为门控矩阵 \(G_{ij} = \frac{\exp(-D_{ij}^2/(2\sigma^2))}{\sum_{r}\exp(-D_{ir}^2/(2\sigma^2))}\)，每行和为1。
  4. **亲和度校准与密集重建**：用最终层query-query相似度 \(A^h = \text{softmax}(Q^h(Q^h)^T/\sqrt{d_h})\)，然后通过 \(G\) 校准：\(\tilde{A}^h_{ij} = \frac{(A^h_{ij}+\varepsilon)(G_{ij}+\varepsilon)^\lambda}{\sum_r (A^h_{ir}+\varepsilon)(G_{ir}+\varepsilon)^\lambda}\)。用校准后的亲和度聚合值特征 \(V^h\)，再经后处理得到最终密集预测。

## 3. 实验设计
- **数据集与场景**：零样本语义分割，8个标准基准：
  - 无背景类：VOC20、Context59、COCO-Stuff、Cityscapes、ADE20K。
  - 有背景类：VOC21、Context60、COCO-Object。
- **评估指标**：mIoU（平均交并比）。
- **对比方法**：
  - 训练自由的基线：CLIP、MaskCLIP、ReCo、SCLIP、ClearCLIP、NACLIP（NACLIP结果取自原论文）。
  - 非训练自由基线：GroupViT、TCL（需要训练，但仅作为参考）。
- **主干网络**：CLIP ViT-B/16 和 ViT-L/14，均冻结，无参数更新。
- **提示**：使用OpenAI ImageNet提示模板平均文本特征。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量或训练时长。原因：TraceCLIP是无训练方法，所有实验结果均来自冻结的CLIP模型，无需额外训练。推理阶段可能使用单GPU（如V100/A100），但论文未提供具体信息。

## 5. 实验数量与充分性
- **实验数量**：包含三类实验：
  1. **主要结果（表3）**：在8个数据集上对比10种方法（包括2个训练方法）在两个主干上，共16个对比组合。
  2. **诊断实验（表1, 2）**：在COCO验证集上分析不同特征来源（最终补丁、值特征、不同层贡献特征）的区域分类精度、点定位精度、内外响应差距；以及最后块内各组件的贡献分析。
  3. **消融与分析（表4）**：密集读出的层选择分析（a）；组件消融（b）比较直接读V(-1)、Ah加权、G加权、完整TraceCLIP。
  4. **定性可视化（图2）**：层间响应热力图。
- **充分性评价**：实验设计较为充分且客观：
  - 覆盖多种数据集（室内、室外、城市场景、物体背景等），结果具有代表性。
  - 与多种训练自由及有训练方法公平对比，且报告了不同主干下的性能。
  - 消融实验合理验证了贡献特征有效性、亲和度与拓扑门的各自作用。
  - 但缺少**统计显著性测试**或**多次运行标准差**报告；无**不同超参数敏感性分析**（β, λ, σ等）；未在**更多下游任务**（如开放词汇检测、短语定位）上验证。

## 6. 论文的主要结论与发现
1. Patch-to-CLS贡献特征相比最终补丁状态在区域分类、空间定位上显著优越（ViT-B/16区域准确率43.1% vs 11.3%，点定位65.7% vs 19.9%）。
2. 局部语义证据在自注意力的贡献路径中清晰暴露，但在残差连接和MLP混合后变得不易直接访问。
3. 基于贡献的语义-测地线门可以有效抑制跨语义边界的特征传播，与视觉亲和度互补，提升密集预测性能。
4. 在八个零样本语义分割基准上，TraceCLIP相比之前最强的训练自由方法平均mIoU提升1.3-4.5个百分点（ViT-B/16: 39.8 vs 38.2 无背景, 42.7 vs 41.4 有背景; ViT-L/14: 39.0 vs 34.5, 38.4 vs 35.5）。
5. 保持CLIP完全冻结且无需外部模型，证明全局对齐的内部构造中可以恢复局部语义。

## 7. 优点
- **创新性**：首次明确探究并利用CLIP内部patch-to-CLS贡献路径作为局部语义来源，而非仅仅依赖补丁状态变换。
- **无训练且高效**：整个框架不引入任何可学习参数或外部基础模型，完全训练自由，易于部署。
- **通用性**：在两种主干、有无背景的设置下均稳定提升，且方法不依赖特定主干结构。
- **诊断实验扎实**：通过区域分类、点定位、内外差距和组件分析，直观证明贡献路径的有效性，为后续工作提供理论基础。
- **设计巧妙**：语义-测地线门将离散语义标签和置信度转换为连续的距离门控，与注意力校准自然结合，既利用全局几何结构又不损失边界信息。

## 8. 不足与局限
- **实验覆盖有限**：仅验证了零样本语义分割任务，未在开放词汇检测、短语定位等密集任务上测试。
- **超参数敏感性未知**：β、λ、σ等超参数固定取值，未详细报告在不同数据集或分辨率下的敏感性，可能导致实际部署时需调参。
- **计算效率**：短路径距离计算（全对）复杂度 \(O(N^3)\)，对于大图像（如高分辨率滑动窗口）可能带来推理负担，论文未分析时间开销。
- **依赖CLS注意力权重**：贡献特征需要每个补丁的注意力权重，若未来CLIP变体改变注意力结构（如无CLS token），方法需调整。
- **缺少与有训练方法的公平比较**：虽然对比了GroupViT和TCL，但后者使用不同主干（ViT-S/16）或经过训练，直接用于零样本评估不公平，论文未做严格控制变量。
- **无统计保证**：未提供多次运行的平均值和标准差，难以判断改进的显著性。

（完）
