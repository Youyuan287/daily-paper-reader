---
title: Training-Free Open-Vocabulary 3D Point-Cloud Segmentation on the Generalized Few-Shot Benchmark
title_zh: 基于广义少样本基准的无训练开放词汇3D点云分割
authors: "Silas kwabla Gah, Ebenezer Owusu"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15331v1"
tags: ["query:open-vocab-d"]
score: 10.0
evidence: 使用冻结视觉语言模型的免训练开放词汇3D点云分割
tldr: "广义少样本3D点云分割需同时处理基类和新类，现有方法依赖训练和标签。本文提出全训练无关的开词汇管道，冻结RegionPLC提供密集先验，SAM3通过类别名提示分割，再利用跨视图一致性投票决定点归属新类。在ScanNet200上，新类mIoU提升+2.6而基类精度仅降0.5，恢复新类与全监督方法33%的差距。在ScanNet++上，新类mIoU从16.2跃升至31.9，谐波均值提升9.6。证明无需训练、标签或支持样本即可有效分割，尤其在弱先验场景下优势显著。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15331v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1186, \"height\": 755, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15331v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1225, \"height\": 496, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15331v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1257, \"height\": 1098, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15331v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1097, \"height\": 284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15331v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1097, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15331v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 748, \"height\": 232, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15331v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 598, \"height\": 182, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15331v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 420, \"height\": 341, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15331v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 597, \"height\": 179, \"label\": \"Table\"}]"
motivation: 探索在不使用训练、3D标签和少样本支持的情况下，仅通过冻结视觉语言模型和跨视图一致性实现开词汇分割。
method: 冻结RegionPLC作为3D视觉语言先验，SAM3作为提示概念分割器，通过跨视图一致性投票确定点是否属于新类。
result: ScanNet200上新类mIoU+2.6，基类精度降0.5；ScanNet++上新类mIoU从16.2升至31.9，谐波均值从21.5升至31.1。
conclusion: 训练无关管道在无需任何监督下有效分割新类，优于需训练方法，尤其在弱先验场景下表现突出。
---

## 摘要
广义少样本3D点云分割（GFS-PCS）要求模型将场景分割成训练时见过的多个基类和新类别。当前最优方法通过将密集但有噪声的3D视觉语言先验与少样本支持相结合来应对新类别，但代价是基类3D标签、每个episode的训练以及支持注释本身。我们探究在没有这些（无训练、无3D标签、甚至无少样本支持）的情况下，同样的结合能走多远。我们将冻结的3D视觉语言模型（RegionPLC）作为密集先验，与冻结的可提示概念分割器（SAM3）配对，由裸新类名称提示并从带有姿态的RGB视图提升，通过跨视图一致性来调和两者：只有当足够多的看到该点的视图一致时，该点才成为新点。在ScanNet200 GFS-PCS基准上，这个完全无训练的开放词汇流水线在保持基类精度0.5以内的情况下，将新类mIoU比无训练密集先验提高了+2.6，并恢复了与使用更多监督的最先进训练方法之间三分之一的差距。我们进一步表明，将少样本支持注入流水线（作为融合门控和原型密集分类器）相对于仅有一致性并没有增加任何东西，实际上通过分类器降低了性能，这就是为什么该方法根本不需要支持。在更难的ScanNet++基准上，密集先验在新类别上弱得多，同一流水线在基类代价1.7的情况下几乎将新类mIoU翻倍（+15.7，从16.2到31.9），将调和平均从21.5提升到31.1。

## Abstract
Generalized few-shot 3D point-cloud segmentation (GFS-PCS) asks a model to segment a scene into many base classes seen at training time and a set of novel classes. The state of the art reaches novel classes by reconciling a dense but noisy 3D vision-language prior with the few-shot support, but it pays for this with base 3D labels, per-episode training, and the support annotations themselves. We ask how far the same reconciliation can go with none of these: no training, no 3D labels, and not even the few-shot support. We pair a frozen 3D vision-language model (RegionPLC) as a dense prior with a frozen promptable concept segmenter (SAM3), prompted by the bare novel class names and lifted from posed RGB views, and reconcile the two by cross-view consistency: a point becomes novel only when enough of the views that see it agree. On the ScanNet200 GFS-PCS benchmark this fully training-free, open-vocabulary pipeline improves novel mIoU by +2.6 over the training-free dense prior while holding base accuracy within 0.5, and recovers a third (33%) of the novel-class gap to the trained state of the art that uses far more supervision. We further show that injecting the few-shot support into the pipeline, as a fusion gate and as a prototypical dense classifier, adds nothing over consistency alone and in fact degrades it through the classifier, which is why the method needs no support at all. On the harder ScanNet++ benchmark, where the dense prior is far weaker on novel classes, the same pipeline nearly doubles novel mIoU (+15.7, from 16.2 to 31.9) at a 1.7 base cost, lifting the harmonic mean from 21.5 to 31.1

---

## 论文详细总结（自动生成）

# 基于广义少样本基准的无训练开放词汇3D点云分割

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：广义少样本3D点云分割（GFS-PCS）要求模型同时预测训练时见过的基类和新类别，但现有方法依赖训练、3D基类标签和少样本支持（每个新类仅几个样本），导致部署成本高。本文探究在**完全无训练、无3D标签、无少样本支持**的极端约束下，通过结合冻结的3D视觉语言密集先验与2D可提示概念分割器，能否有效进行开放词汇分割。
- **整体含义**：证明基于跨视图一致性调和两种互补信息源（密集但噪声、稀疏但精确）足以在弱监督场景下显著提升新类性能，为3D场景理解提供了一种低成本的训练无关基线。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：利用冻结的3D视觉语言模型（RegionPLC）作为密集先验，提供每点的开放词汇预测；利用冻结的2D概念分割器（SAM3）以新类名称作为提示，从带有相机姿态的RGB帧中提取2D掩码并投影到3D点云，得到稀疏且精确的新类证据；通过**跨视图一致性**（cross-view consistency）调和两者——只当足够多的视图同意该点为某新类时，才重写密集预测。
- **关键技术细节**：
  - **密集分支**：RegionPLC输出每点对数 \( f_d(P) \)，取 argmax 得密集标签 \( d_i \)，置信度 \( \kappa_i \)。
  - **稀疏分支（SAM3 lifted）**：对每个新类名称和每帧，SAM3输出概念掩码 \( M_t^n \)；将3D点投影到各帧，统计该点被看到的帧中掩码同意的比例 \( s_i(n) = \frac{\#同意帧}{\#可见帧} \)，取最大值 \( s_i \) 和对应类别 \( \hat{n}_i \)。
  - **调和规则**：若 \( s_i \ge \tau \) 则重标为该新类，否则保留密集预测。\(\tau\) 是唯一超参数，按基准校准（ScanNet200: 0.5, ScanNet++: 0.15）。
  - **无训练**：所有模块冻结，不进行任何微调或优化。

## 3. 实验设计：数据集、Benchmark、对比方法
- **数据集与基准**：
  - **ScanNet200 GFS-PCS**: 12基类 / 45新类，312个验证场景。
  - **ScanNet++ GFS-PCS**: 12基类 / 18新类，50个验证场景（其中48有DSLR图像）。
- **对比方法**：
  - 密集先验单独（RegionPLC零样本）——训练无关基线。
  - 朴素稀疏覆盖法（任何提升的S AM3掩码直接覆盖密集预测）。
  - 有监督SOTA：GFS-VL（5-shot，训练于3D基类标签和支持，作为上限参考，非击败目标）。
- **评估指标**：基类mIoU、新类mIoU、全类mIoU、基-新调和均值（HM）。

## 4. 资源与算力
- 所有实验在**单个NVIDIA T4 GPU**上运行（无训练或微调）。
- 没有训练时长，仅有推理时间：ScanNet++约20秒/场景，ScanNet200约3秒/场景（受SAM3的2D前向和投影主导）。
- 说明：论文明确声明“nothing is trained or fine-tuned”，算力需求极低。

## 5. 实验数量与充分性
- **实验组数**：
  - 主实验：两个数据集上的完整验证。
  - 消融实验：一致性阈值τ扫描（6个值）、辅助门控（视图计数、密集置信度）、支持注入（两个位点L1融合门控、L2原型分类器，每个含多个参数设置）。
  - 统计显著性：配对bootstrap分析（10⁴次重采样）给出95%置信区间。
  - 帧率/预算消融：不同框架密度对性能的影响。
  - 每类细节分析：列出性能提升最大的类别。
- **充分性与公平性**：
  - 完全遵循GFS-VL的协议和基准，直接可比。
  - τ预提交为保守值，在评估集上仅报告一次，未调优最大化指标（但补充材料说明了选择协议）。
  - 支持注入实验预声明零点假设，用置信区间判断是否零。
  - 但：ScanNet++无独立开发集，τ在一个探测场景上确定，存在轻微泄漏风险（论文讨论了）。
  - 总体实验设计严谨，覆盖了关键变量。

## 6. 论文的主要结论与发现
- **自动验证**：跨视图一致性是调和的关键；朴素覆盖会牺牲基类精度。
- **支持非必要**：通过融合门控或原型分类器注入少样本支持，效果为无效或负向（L1零，L2显著负），证明在共识下支持无额外收益。
- **性能提升**：
  - ScanNet200：新类mIoU +2.6（基类仅降0.5），恢复与GFS-VL差距的33%。
  - ScanNet++：新类mIoU +15.7（16.2→31.9），HM +9.6（21.5→31.1），接近GFS-VL的HM（31.82）。
- **提升来源**：小物体、长尾类（如百叶窗、窗台、电话、烟雾探测器等），密集先验几乎为零的类被2D分割器成功识别并共识验证。

## 7. 优点
- **极简监督**：使用最少输入（仅类别名称和RGB-D捕获），无需任何3D标注、训练或支持样本。
- **全冻结**：所有组件为零样本，无参数学习，部署成本极低。
- **跨视图一致性的有效性**：有效抑制2D分割器的过火错误，保留基类精度。
- **全面的消融实验**：系统验证了多个因子（阈值、门控、支持位点、帧率）的影响，置信区间的统计检验增加可信度。
- **开源友好**：使用公开模型（RegionPLC、SAM3）和数据，无需额外收集。

## 8. 不足与局限
- **基类差距**：缺乏3D训练导致基类性能落后于GFS-VL约20个百分点（ScanNet200 46.44 vs 67.57）。
- **依赖文本提示**：仅对新类名称有效，对“物体”“天花板”等非具体名词效果差；未探索同义词或模板集合。
- **τ需基准校准**：跨视图一致性的尺度受采样密度影响，需为每个基准预调一个阈值（从保守值中选择）。
- **支持注入测试不完整**：未测试通过示例条件（L3）输入的支持（因SAM3接口不支持）；两个测试位点均通过RegionPLC特征路由，不能完全排除共享特征解释。
- **计算依赖**：需要每帧运行SAM3概念分割，对于大规模场景或高帧率可能耗时。
- **未与实例级开放词汇方法直接对比**：如OpenIns3D、OV-SAM3D等，但论文解释了直接语义比较的困难。
- **实验覆盖**：未在其他数据集（如室外nuScenes）上验证；未与更强的密集模型（如SOLE、UniM-OV3D）组合。

（完）
