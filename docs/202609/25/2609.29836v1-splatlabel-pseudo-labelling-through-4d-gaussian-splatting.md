---
title: "SplatLabel: Pseudo-Labelling through 4D Gaussian Splatting"
title_zh: SplatLabel：通过4D高斯泼溅进行伪标签生成
authors: "Nitya Nanvani, Andras Palffy, Holger Caesar"
date: 2026-09-24
pdf: "https://arxiv.org/pdf/2609.29836v1"
tags: ["query:open-vocab-d"]
score: 8.0
evidence: 用4D高斯表示把2D视觉基础模型先验提升到3D语义分割与占据栅格
tldr: 2D视觉基础模型能为3D语义伪标签提供先验，但迁移到稳健3D表示通常依赖复杂启发式或多模型集成。SplatLabel提出基于4D高斯表示的自动化流水线，通过显式时间流形建模3D基元轨迹与生命周期，无需预标注3D框即可跟踪动态目标。它融合360度LiDAR虚拟深度与2D模型连续软概率，输出带置信度的LiDAR分割及任意体素语义占用栅格。在SemanticKITTI上多个召回水平持续超越SOTA，并用广义风险-召回指标评估伪标签质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.29836v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1737, \"height\": 664, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.29836v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1825, \"height\": 806, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.29836v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 871, \"height\": 657, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.29836v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1666, \"height\": 335, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.29836v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1820, \"height\": 516, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.29836v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1609, \"height\": 382, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.29836v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 795, \"height\": 299, \"label\": \"Table\"}]"
motivation: 2D基础模型先验迁移到3D伪标签时，常受动态场景、未观测区域和语义歧义影响，且依赖复杂启发式或多模型集成。
method: SplatLabel以4D高斯表示为核心，通过显式时间流形建模3D基元轨迹与生命周期，并融合360度LiDAR虚拟深度和2D模型软概率，输出带置信度的LiDAR分割与任意体素语义占用。
result: 在SemanticKITTI上，SplatLabel在多个召回水平上持续优于现有最优基线，并在LiDAR分割与占用预测任务中验证了鲁棒性。
conclusion: 该工作无需预标注3D框，将伪标签评估重构为选择性分类与广义风险-召回，为动态3D场景的自动化标注提供稳健框架。
---

## 摘要
尽管2D视觉基础模型为自动化3D语义伪标签提供了一条途径，但将这些先验转化为稳健的3D表示通常需要复杂的启发式方法或多模型集成。我们提出SplatLabel，一种自动化流程，利用4D高斯表示来提取带有预测置信度的LiDAR分割，以及任意体素分辨率下的语义占用栅格。其核心在于，SplatLabel通过显式时间流形处理动态环境，该流形对单个3D基元的轨迹和生命周期进行建模。这使系统能够准确跟踪运动目标，并严格定义物体出现和消失的时间，完全无需预先标注的3D边界框。为了稳健地支持这种动态跟踪，该表示由结构和语义先验作为基础：我们通过虚拟深度图集成360度LiDAR来引导未观测区域的场景几何，并且不依赖特定领域的提示工程，而是直接从2D模型中蒸馏连续软概率，以本质上解决时间和空间上的语义歧义。最后，为了准确反映现实世界中精确率与召回率之间的权衡，我们将伪标签评估重新定义为使用广义风险-召回率指标的选择性分类任务。在SemanticKITTI上的实验表明，SplatLabel在多个召回水平上持续优于最先进的基线，为3D LiDAR分割和占用预测建立了一个高度稳健的框架。

## Abstract
While 2D Vision Foundation Models offer a pathway to automate 3D semantic pseudo-labelling, translating these priors into robust 3D representations typically requires complex heuristics or multi-model ensembles. We introduce SplatLabel, an automated pipeline that leverages a 4D Gaussian representation to extract LiDAR segmentation with predictive confidence, as well as semantic occupancy grids at arbitrary voxel resolutions. At its core, SplatLabel handles dynamic environments through an explicit temporal manifold that models the trajectories and lifespans of individual 3D primitives. This allows the system to accurately track moving actors and strictly define when objects appear and disappear, completely eliminating the need for pre-annotated 3D bounding boxes. To robustly support this dynamic tracking, the representation is grounded by structural and semantic priors: we guide scene geometry in unobserved regions by integrating 360-degree LiDAR via virtual depth maps, and rather than relying on domain-specific prompt engineering, we directly distill continuous soft probabilities from 2D models to inherently resolve semantic ambiguities over time and space. Finally, to accurately reflect the real-world trade-off between precision and recall, we reframe pseudo-label evaluation as a selective classification task using a generalized risk-recall metric. Experiments on SemanticKITTI demonstrate that SplatLabel consistently outperforms state-of-the-art baselines across multiple recall levels, establishing a highly robust framework for both 3D LiDAR segmentation and occupancy prediction.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- **研究动机**：自动驾驶与自主系统需要全面的3D语义感知，但人工标注3D数据成本极高。2D视觉基础模型（VFMs）为自动化3D语义伪标签提供了可能，但现有方法存在瓶颈。
- **关键瓶颈**：
  - 动态场景建模通常依赖预标注3D边界框来分离运动目标与静态背景，形成“为免标注而需标注”的循环依赖。
  - 将2D语义先验提升到3D时，常需复杂启发式、多模型集成或计算昂贵的球面光栅化，以应对时序不一致与遮挡。
  - 现有伪标签评估只统计“已标注点”上的精度，忽略不同方法召回率差异以及精度-召回权衡。
- **整体含义**：论文提出 **SplatLabel**，一个基于4D高斯泼溅的自动化伪标签管线，通过显式时间流形建模动态目标轨迹与生命周期，无需3D框即可跟踪运动目标，并融合360度LiDAR几何与2D连续软语义，输出带置信度的LiDAR分割和任意体素语义占用。同时将伪标签评估重构为选择性分类任务，使用类别平均AUGRC衡量风险-召回可靠性。

## 2. 方法论
- **核心思想**：将场景表示为受显式时间流形控制的连续4D高斯场；把运动、时间寿命和存在边界参数化为单个3D高斯基元的内在属性，统一空间表示、结构致密化与运动学跟踪。
- **时空蒸馏与几何先验**：
  - 放弃脆弱的光度梯度致密化，采用基于MCMC/SGLD的随机状态转移动态调节基元容量。
  - **连续语义蒸馏**：直接从SAM 3提取原始logits，查询数据集类别名，避免复杂提示工程；将语义损失反传到高斯参数，并在整个时空序列上聚合梯度，平滑帧间闪烁、解决类别歧义。
  - **全向几何监督**：提出“虚拟深度图”，在未观测区域放置人工相机，将完整LiDAR点云投影为虚拟深度目标；同时把LiDAR深度线索作为结构采样先验，在高结构不一致区域主动生成和调整基元。
- **时间流形参数化**：
  - **双变形核**：用n次多项式与n次Fourier噪声网络建模平滑线性路径和高频运动：
    \[
    \Delta(t)=\sum_{k=1}^n \alpha_k t^k+\sum_{m=1}^n \beta_m \sin(2\pi m t)+\gamma_m \cos(2\pi m t)
    \]
  - **门控变形掩码**：Sigmoid门控掩码将静态背景的变形核钳制为零；并将掩码作为动态加权重定位机制，缩放基元重生概率，优先沿动态轨迹采样。
  - **变形耦合寿命**：每个高斯有基础寿命 \(L_{base}\)，与门控掩码 \(M_{deform}\) 耦合：
    \[
    L_{eff}=(1-M_{deform})+M_{deform}\cdot L_{base}
    \]
    静态基元保留全序列足迹，动态基元受局部寿命限制；低于阈值的基元被剔除并重生。
  - **时间存在激活**：
    \[
    \alpha_t=\text{sigmoid}\left(k\cdot\left(1-\frac{|t-\tau|}{L_{eff}}\right)\right)
    \]
    作为基础不透明度的乘子，\(\tau\) 为学习的时间中心，\(k\) 为渐进锐度参数，训练后期锐化为刚性时间边界。
  - **轨迹保持的时间解耦**：时间探索与时空克隆会错位多项式/Fourier系数；提出全局时间解耦变形核：
    \[
    \Delta_{eff}(t,\tau)=\Delta(t)-\Delta(\tau)
    \]
    更新 \(\tau\) 时对基参考位置施加解析偏移 \(\Delta(\tau')-\Delta(\tau)\)，保证时间窗口优化不破坏已收敛
